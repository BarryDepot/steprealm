# Address of the StepRealm API.
# On a physical phone, localhost means the phone — use your development
# machine's LAN address (macOS: ipconfig getifaddr en0) or the deployed URL.
EXPO_PUBLIC_API_URL=http://192.168.1.20:3000
